# Copy to .env.production and load it with:
# docker compose --env-file .env.production up -d --build
APP_PORT=80
APP_IMAGE_TAG=prod
# Use Open-Topo-Data by default. Replace only if you host your own compatible API.
VITE_API_BASE_URL=https://api.opentopodata.org/v1/srtm30m
