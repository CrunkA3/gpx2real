# Copy to .env.production and load it with:
# docker compose --env-file .env.production up -d --build
APP_PORT=80
APP_IMAGE_TAG=prod
OPENTOPODATA_IMAGE=opentopodata:1.7.0
# Uses the local OpenTopoData service via the same-origin Nginx proxy.
VITE_API_BASE_URL=/elevation/v1/srtm90m
